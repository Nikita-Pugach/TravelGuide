@model TravelGuide.Models.Entities.Agency
@{
    ViewData["Title"] = "Редактирование агентства";
}

<div class="row justify-content-center">
    <div class="col-md-8">
        <div class="card">
            <div class="card-header">
                <h4>✏️ Редактирование агентства</h4>
            </div>
            <div class="card-body">
                <form asp-action="EditAgency" method="post" enctype="multipart/form-data">
                    @Html.AntiForgeryToken()
                    <input type="hidden" asp-for="Id" />
                    
                    <div class="mb-3">
                        <label asp-for="Name" class="form-label">Название *</label>
                        <input asp-for="Name" class="form-control" required />
                        <span asp-validation-for="Name" class="text-danger"></span>
                    </div>
                    
                    <div class="mb-3">
                        <label asp-for="ContactInfo" class="form-label">Контактная информация *</label>
                        <textarea asp-for="ContactInfo" class="form-control" rows="2" required></textarea>
                    </div>
                    
                    <div class="mb-3">
                        <label asp-for="Description" class="form-label">Описание</label>
                        <textarea asp-for="Description" class="form-control" rows="3"></textarea>
                    </div>
                    
                    @if (!string.IsNullOrEmpty(Model.LogoUrl))
                    {
                        <div class="mb-3 text-center">
                            <img src="@Model.LogoUrl" alt="@Model.Name" class="img-thumbnail" style="max-height: 100px;" />
                        </div>
                    }
                    
                    <div class="mb-3">
                        <label for="logo" class="form-label">Новый логотип</label>
                        <input type="file" name="logo" id="logo" class="form-control" accept="image/*" />
                    </div>
                    
                    <div class="d-flex gap-2">
                        <button type="submit" class="btn btn-primary">💾 Сохранить</button>
                        <a asp-action="Agencies" class="btn btn-outline-secondary">Отмена</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>